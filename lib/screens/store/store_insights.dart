import 'package:flutter/material.dart';

class StoreInsights extends StatelessWidget {
  const StoreInsights({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Store Insights (Graph)'));
  }
}
